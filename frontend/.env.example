# Copy this file to .env and fill in the value.
# In development, leave this empty — the Vite proxy handles it.
# In production (after deploying the backend to Render), paste the Render URL here.
VITE_API_URL=https://your-backend.onrender.com
